// ExpertiseType.ts
export interface ExpertiseType {
    id?: string;
    icon: string; // Store the name of the Phosphor Icon
    category: string;
    title: string;
    desc: string;
    shortDesc: string;
}